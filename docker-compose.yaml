services:
  gateway:
    build:
      context: .
      dockerfile: ./apps/gateway/Dockerfile
      target: development
    command: pnpm run start:dev gateway
    ports:
      - 3000:3000
    volumes:
      - .:/usr/src/app
  mongo:
    image: mongo